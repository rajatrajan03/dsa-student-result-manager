services:
  - type: web
    name: student-result-manager
    runtime: static
    buildCommand: ""
    staticPublishPath: .
    pullRequestPreviewsEnabled: false
    routes:
      - type: rewrite
        source: /
        destination: /index.html
